# 16MB flash layout for M5Stack StopWatch.
#
# CRITICAL: the factory-app offset here is a trap. The Arduino ESP32
# build (esptool merge-bin) ALWAYS writes the app to 0x10000 for the
# app3M_fat9M_16MB scheme, regardless of what this file says. So the
# bootloader entry must be an OTA `app0` partition at 0x10000, NOT
# `factory` at 0x20000 (which is a PlatformIO convention that does
# NOT apply here — the bootloader looks at 0x20000, finds no image,
# and refuses to boot, leaving the device at a black panel).
#
# This layout mirrors the IDE default app3M_fat9M_16MB scheme
# (app0 at 0x10000, app1 at 0x310000) and uses the ~9.9 MB tail as a
# raw FAT data region (`badgeimg`) that is exposed over USB MSC when
# the USB Drive app runs. The device mounts it READ-ONLY (no wear
# levelling): the host PC is the only writer, so the raw LBA layout
# the PC sees is exactly what the device parses on remount.

nvs,        data, nvs,       0x009000,  0x005000
otadata,    data, ota,       0x00E000,  0x002000
app0,       app,  ota_0,     0x010000,  0x300000
app1,       app,  ota_1,     0x310000,  0x300000
badgeimg,   data, fat,       0x610000,  0x9E0000
coredump,   data, coredump,  0xFF0000,  0x001000